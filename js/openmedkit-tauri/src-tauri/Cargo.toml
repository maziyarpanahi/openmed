[package]
name = "openmedkit-tauri-sidecar"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = "2"
tauri-plugin-shell = "2"
tokio = { version = "1", features = ["sync"] }
